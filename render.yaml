services:
  - type: web
    name: petpals-backend
    env: node
    plan: starter
    rootDir: ./backend
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
      - key: MONGODB_URI
        fromDatabase:
          name: petpals-db
          property: connectionString

  - type: web
    name: petpals-frontend
    env: static
    plan: starter
    rootDir: ./frontend
    buildCommand: npm install && npm run build
    staticPublishPath: ./dist
    envVars:
      - key: VITE_API_URL
        value: https://petpals-backend.onrender.com

databases:
  - name: petpals-db
    plan: starter